# Personal config override - see config/routefetch.conf.default.
# Copy that file to config/routefetch.conf to customize DMG mode, tier
# weights, or shiny odds without it ever becoming a tracked/committed file.
config/routefetch.conf

# Alternate config file location, if ROUTEFETCH_CONFIG is pointed here
*.local.conf

# Local PokeAPI cries clone (~55MB audio), if fetched while prototyping the
# Cries roadmap item - see the Roadmap section. Not vendored either way.
data/cries/

# OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor/IDE
*.swp
*.swo
*~
.vscode/
.idea/

# Shell script artifacts
*.log
*.tmp
